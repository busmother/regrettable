
<b><%= @incident.name%></b>
<br>
<br>
<% if @incident.apologized?(current_user) && @incident.apologies.count > 2%>
    <em><%link_to current_user.name, current_user%>, <%link_to @incident.apologies.first.users.first.name, @incident.apologies.first.users.first%> and <%=@incident.apologies.users.count-2%> others apologize for this:</em>
<% elsif @incident.apologized?(current_user) && @incident.apologies.count == 2 %>
    <em><%link_to current_user.name, current_user%> and <%link_to @incident.apologies.first.users.first.name, @incident.apologies.first.users.first%> apologize for this:</em>
<% elsif @incident.apologized?(current_user) && @incident.apologies.count == 1 %>
    <em><%link_to current_user.name, current_user%> apologize for this:</em>
<% elsif !@incident.apologized?(current_user) && @incident.apologies.count > 3%>
    <em><%= link_to @incident.apologies.first.users.first.name, @incident.apologies.first.users.first%> and <%=@incident.apologies.count-1%> others apologize for this.</em>
<% elsif !@incident.apologized?(current_user) && @incident.apologies.count == 2%>
    <em><%= link_to @incident.apologies.first.users.first.name, @incident.apologies.first.users.first %> and <%= link_to @incident.apologies.last.users.last.name, @incident.apologies.last.users.last%> apologize for this.</em>
<% elsif !@incident.apologized?(current_user) && @incident.apologies.count == 1%>
    <em><%= link_to @incident.apologies.first.users.first.name, @incident.apologies.first.users.first %> apologizes for this.</em>
<% else %>
    <em>No one has apologized for this. Will you be first?.</em>
<%end%>
<br>
<br>
[apology partial here]
<br>
<br>
<%= render '%>
